# test
#include<stdio.h>

int main(void)
{
    int length,width,height,volumn;
    length = 10;
    width = 5;
    height = 10;
    volumn=length*width*height;
    printf("volumnis %d\n",volumn);
    return 0;
}
